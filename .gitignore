# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache
/e2e/.auth

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma - keep migrations tracked in git

# uploads
/uploads

# nginx dev certificates
nginx/certs/
.claude/worktrees/
.idea/

# Slidev build output for the pitch deck — regenerate with `npm run build`
# inside docs/pitch/ rather than checking compiled bundles into the repo.
docs/pitch/dist/

# Local-only Docker Compose overrides (machine-specific ports/env). Auto-merged
# by `docker compose`, so committing it would wrongly apply on the server too.
docker-compose.override.yml

# Playwright MCP scratch output (snapshots/screenshots) — tooling artifact
.playwright-mcp/
