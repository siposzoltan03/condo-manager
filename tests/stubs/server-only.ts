// Test stub for the `server-only` package. The real package throws when
// imported outside a server-component context — that protection is moot in
// tests, where we explicitly load server-side code to characterize it.
export {};
